#include <stdio.h>
#include <stdint.h>
#include <unistd.h>
#include <sys/types.h>
#include <sys/stat.h>

#include "utils.h"

int main() {


  // fwrite(&buf, 1, 5, fd);
  // fseek(fd, 0, SEEK_SET);

  // printf("%d\n", read_uint16_littleendian(fd));
  // printf("%d\n", read_uint16_bigendian(fd));


  return 0;
}
